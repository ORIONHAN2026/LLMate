"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.make = void 0;
const sass = require("sass");
function getProperty(obj, name) {
    name = name.replace(/\[(\w+)]/g, '.$1').replace(/^\./, '');
    const split = name.split('.');
    let key = split.shift();
    if (!key || !Object.prototype.hasOwnProperty.call(obj, key))
        return '';
    let result = obj[key];
    const len = split.length;
    if (!len)
        return result || '';
    if (typeof result !== 'object')
        return '';
    for (let i = 0; i < len; i++) {
        key = split[i];
        if (!Object.prototype.hasOwnProperty.call(result, key))
            return '';
        result = result[split[i]];
        if (typeof result !== 'object')
            return result;
    }
    return result;
}
const make = async function (data, options) {
    if (data.path == null) {
        return '';
    }
    const self = this;
    const themeCfg = self.theme.config || {};
    const config = Object.assign(this.theme.config.sass || {}, this.config.sass || {}, {
        functions: {
            'hexo-theme-config($ckey)': function ([arg]) {
                if (!(arg instanceof sass.SassString)) {
                    return sass.sassNull;
                }
                const val = getProperty(themeCfg, arg.text);
                if (Array.isArray(val)) {
                    const vals = val.map(x => new sass.SassString(x));
                    return new sass.SassList(vals);
                }
                return new sass.SassString(val, { quotes: false });
            },
            'hexo-config($ckey)': function ([arg]) {
                if (!(arg instanceof sass.SassString)) {
                    return sass.sassNull;
                }
                const val = getProperty(self.config, arg.text);
                if (Array.isArray(val)) {
                    const vals = val.map(x => new sass.SassString(x));
                    return new sass.SassList(vals);
                }
                return new sass.SassString(val, { quotes: false });
            },
        },
    });
    return sass.compileAsync(data.path, config).then(result => result.css);
};
exports.make = make;
